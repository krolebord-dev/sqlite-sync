{
  "name": "benchmarks",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsgo --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsgo --noEmit",
    "benchmark:crdt-event-throughput": "node ./scripts/run-crdt-event-throughput.mjs",
    "benchmark:sqlite-kv-store-rowid": "node ./scripts/run-sqlite-kv-store-rowid.mjs",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "@sqlite-sync/core": "workspace:*",
    "@sqlite.org/sqlite-wasm": "catalog:",
    "kysely": "^0.28.10",
    "tinybench": "^2.9.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
